# Day 3 Quiz: Status Codes

Answer before checking `docs/answers/day-03-answers.md`.

1. What does `200` mean?
2. What does `201` mean?
3. What is the difference between `401` and `403`?
4. When should an API return `404`?
5. Why is `500` a server-side problem?
