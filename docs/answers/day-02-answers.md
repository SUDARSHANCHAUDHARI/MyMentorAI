# Day 2 Answers: HTTP Methods

1. `GET`.
2. `POST`.
3. `PUT` usually replaces the full resource. `PATCH` updates part of the resource.
4. Idempotent means repeating the same request leaves the server in the same final state.
5. `GET` is safe because it should only read data and not change server state.
