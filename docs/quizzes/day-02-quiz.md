# Day 2 Quiz: HTTP Methods

Answer before checking `docs/answers/day-02-answers.md`.

1. Which HTTP method should be used to read data?
2. Which HTTP method is commonly used to create data?
3. What is the practical difference between `PUT` and `PATCH`?
4. What does idempotent mean?
5. Why is `GET` called a safe method?
