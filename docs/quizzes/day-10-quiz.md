# Day 10 Quiz: JSON Validation

Answer before checking `docs/answers/day-10-answers.md`.

1. What is the difference between a JSON object and a JSON array?
2. Why should API tests check data types?
3. What is a required field?
4. Why is checking only "response is not empty" weak?
5. Name one nested JSON value you could validate.
